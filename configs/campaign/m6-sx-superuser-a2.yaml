# M6 Study A: nested algebra level a2 (11 of 15 ops), causal
# frontier experiment (D-107). Same runtime/model/seed/repair budget at
# every level; only the surface differs — and the excluded ops vanish from
# the planner manual, not just the router.
suite_path: stores/suite-sx-superuser/suite-v3.json
store_path: stores/sx-superuser
out_dir: runs/m6-sx-superuser-a2
split: dev
systems: [ours]
models: ["openai/Qwen/Qwen2.5-14B-Instruct-AWQ"]
seeds: [0]
max_repairs: 3
exclude_ops: ['neighborhood_evolution', 'diff_snapshots', 'aggregate_events', 'version_history']
memory_db: stores/sx-superuser/memory.sqlite
llm_api_base: "http://localhost:8000/v1"
llm_api_key: "EMPTY"
