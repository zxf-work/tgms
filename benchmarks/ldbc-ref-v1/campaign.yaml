# ldbc-ref-v1 campaign — FROZEN 2026-09-14 by lane D1-prep (OSDI'27 Claim C9,
# LDBC generality's reference-correctness gap). Pre-registered before any run.
# Append-only from here: changes land as dated addenda below, never as edits
# to the blocks already frozen. This lane is laptop-only, prep-only — no
# experiment, ssh session, download, or Neo4j instance was run to produce
# this file; every prediction below is derived from the D1-local SF1
# campaign record and the design memo, cited per row, never invented.
#
# Companion: benchmarks/ldbc-ref-v1/RUNBOOK.md (the how). This file is the
# what-was-predicted; the coordinator/xzgpu lane stamps the run of record.
freeze_id: ldbc-ref-v1-2026-09-14
anchor:
  worktree_commit: 85ae2cd   # this lane's own HEAD at freeze time (main >= 85ae2cd)
  design: docs/design/LDBC_REFERENCE_CORRECTNESS_DESIGN_2026-09-13.md   # D1
  correction: docs/design/BI6_TRIAGE_2026-09-14.md                     # D2 — BI6 is a plan defect, not a TGIR-SEMANTIC-GAP
  harness: [scripts/ldbc_snb_params.py, scripts/tgir_ldbc_sf1.py,
            scripts/ldbc_reference_run.py, scripts/ldbc_compare.py]
  runbook: benchmarks/ldbc-ref-v1/RUNBOOK.md

host: xzgpu (40 cores, 93 GB; per docs/eval/REPRODUCE.md — nothing else
      scheduled in the run's window per the coordinator's brief)

substrate:
  dataset: ldbc-sf1 BI initial snapshot, composite-merged-fk (TGMS side) /
           composite-projected-fk (Neo4j side) — same generated SF1 data,
           two serializations, proven equal by the mandatory same-data gate
           (RUNBOOK.md §4.2) before any query runs
  gate_bound_counts: "2,997,352 nodes / 17,196,776 edges"   # design memo §2, citing PAPER_A_EVIDENCE_FREEZE.md §E addendum 2
  no_update_streams: true   # M8 — only initial_snapshot/ is ever read (RUNBOOK.md §4.3)

neo4j:
  version: 5.26.0   # Community; RUNBOOK.md §2.1 for the two-vendor version conflict this resolves
  dialect_citation: "external_workloads/ldbc/bi/neo4j/scripts/vars.sh:9 (NEO4J_VERSION default 5.20.0); scripts/import.sh:67 (neo4j-admin database import full, a 5.x-only subcommand)"
  plugins: [apoc-5.26.0-core]   # bi-10.cypher is the only one of the 24 queries calling into APOC (apoc.path.subgraphNodes)
  config: {heap: 8g, pagecache: 16g, transaction_timeout_s: 600}   # RUNBOOK.md §2.3, reusing the figure the design memo already judged adequate at this scale

parameters:
  source: benchmarks/ldbc-ref-v1/params.json   # scripts/ldbc_snb_params.py::export_bindings(), one binding, two consumers (design memo §3)
  seed: 5724984519806421702                    # CAMPAIGN_SEED, == the D1-local SF1 campaign's seed (benchmarks/results-v1/ldbc-sf1-campaign.json manifest.campaign_seed)
  selection_rule: "first tuple in LDBC's own file order (design memo §3, §A9)"

tie_break_policy: docs/design/LDBC_REFERENCE_CORRECTNESS_DESIGN_2026-09-13.md#4   # scripts/ldbc_compare.py::compare_topk implements it; RUNBOOK.md §8 on why sort_keys must be supplied by hand

# --------------------------------------------------------------------------
# the 24 templates. `d1_local_rows` is the row count TGIR already produced
# at SF1 (benchmarks/results-v1/ldbc-sf1-campaign.json), cited as the basis
# for each prediction below — never re-measured here. `null` means "never
# run" (IS1/IS4/IS5, post-freeze additions) or "no SF1 record for this exact
# artifact" (BI6.v2, whose predecessor BI6 ERRORED).
# --------------------------------------------------------------------------
templates:
  - id: BI3
    contract: REQUIRES_TOP_K
    plan_artifact: BI3.json
    cypher: bi/neo4j/queries/bi-3.cypher
    param_source: bi-3.csv row 1
    d1_local_rows: 20
    risk: "var-length REPLY_OF*0.. carve (design memo §7); TGIR-SEMANTIC-GAP possible, not assumed"
  - id: BI4
    contract: REQUIRES_TOP_K
    plan_artifact: BI4.json
    cypher: bi/neo4j/queries/bi-4.cypher
    param_source: bi-4.csv row 1
    d1_local_rows: 100
  - id: BI6
    contract: REQUIRES_TOP_K
    plan_artifact: BI6.v2.json   # NOT BI6.json — see BI6_TRIAGE_2026-09-14.md; BI6.json kept as the v1-defect evidence, run and reported separately (RUNBOOK.md §6)
    cypher: bi/neo4j/queries/bi-6.cypher
    param_source: bi-6.csv row 1
    d1_local_rows: null   # v1 (BI6.json) ERRORED at SF1 (null join key 'likerId', 535 rows); v2 has no SF1 record yet
    note: "predicted AGREE, not TGIR-SEMANTIC-GAP — D2's triage verdict is 'plan defect', repaired in BI6.v2.json, fixture-verified against v1 wherever v1 runs (BI6_TRIAGE_2026-09-14.md §6)"
  - id: BI7
    contract: REQUIRES_TOP_K
    plan_artifact: BI7.json
    cypher: bi/neo4j/queries/bi-7.cypher
    param_source: bi-7.csv row 1
    d1_local_rows: 100
  - id: BI9
    contract: REQUIRES_TOP_K
    plan_artifact: BI9.json
    cypher: bi/neo4j/queries/bi-9.cypher
    param_source: bi-9.csv row 1
    d1_local_rows: 100
  - id: BI10
    contract: REQUIRES_TOP_K
    plan_artifact: BI10.json
    cypher: bi/neo4j/queries/bi-10.cypher
    param_source: bi-10a.csv row 1
    d1_local_rows: 100
    risk: "APOC subgraph traversal (RUNBOOK.md §2.4), most likely template to approach the 600s timeout; var-length/path residual (design memo §7)"
  - id: BI11
    contract: CURRENT_ECQR_FRAGMENT
    plan_artifact: BI11.json
    cypher: bi/neo4j/queries/bi-11.cypher
    param_source: bi-11.csv row 1
    d1_local_rows: 1
  - id: BI12
    contract: REQUIRES_ORDERED_RESULT
    plan_artifact: BI12.json
    cypher: bi/neo4j/queries/bi-12.cypher
    param_source: bi-12.csv row 1
    d1_local_rows: 166
  - id: BI17
    contract: REQUIRES_TOP_K
    plan_artifact: BI17.json
    cypher: bi/neo4j/queries/bi-17.cypher
    param_source: bi-17.csv row 1
    d1_local_rows: 0
    risk: "cascade/time-delta path residual (design memo §7); 0 rows at D1-local makes this a weak anchor either way"
  - id: BI18
    contract: REQUIRES_TOP_K
    plan_artifact: BI18.json
    cypher: bi/neo4j/queries/bi-18.cypher
    param_source: bi-18.csv row 1
    d1_local_rows: 20
  - id: IC2
    contract: REQUIRES_TOP_K
    plan_artifact: IC2.json
    cypher: interactive_v1/cypher/queries/interactive-complex-2.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive   # excluded from every third-party-parameter claim (design memo §7, PAPER_A_EVIDENCE_FREEZE.md:645-654); agreement here is evidence of correctness only
    d1_local_rows: 20
  - id: IC5
    contract: REQUIRES_TOP_K
    plan_artifact: IC5.json
    cypher: interactive_v1/cypher/queries/interactive-complex-5.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 0
    risk: "path residual named alongside BI10/BI17 (design memo §7)"
  - id: IC6
    contract: REQUIRES_TOP_K
    plan_artifact: IC6.json
    cypher: interactive_v1/cypher/queries/interactive-complex-6.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 10
  - id: IC8
    contract: REQUIRES_TOP_K
    plan_artifact: IC8.json
    cypher: interactive_v1/cypher/queries/interactive-complex-8.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 11
  - id: IC9
    contract: REQUIRES_TOP_K
    plan_artifact: IC9.json
    cypher: interactive_v1/cypher/queries/interactive-complex-9.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 20
  - id: IC11
    contract: REQUIRES_TOP_K
    plan_artifact: IC11.json
    cypher: interactive_v1/cypher/queries/interactive-complex-11.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 0
  - id: IC12
    contract: REQUIRES_TOP_K
    plan_artifact: IC12.json
    cypher: interactive_v1/cypher/queries/interactive-complex-12.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 5
    risk: "path residual named alongside BI10/BI17 (design memo §7)"
  - id: IS1
    contract: CURRENT_ECQR_FRAGMENT
    plan_artifact: IS1.json
    cypher: interactive_v1/cypher/queries/interactive-short-1.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: null   # post-freeze addition (Lane D2, 2026-09-14); never run at SF1
    note: "single-row profile lookup; predicted trivially agreeing given a valid anchor"
  - id: IS2
    contract: REQUIRES_TOP_K
    plan_artifact: IS2.json
    cypher: interactive_v1/cypher/queries/interactive-short-2.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 10
  - id: IS3
    contract: REQUIRES_ORDERED_RESULT
    plan_artifact: IS3.json
    cypher: interactive_v1/cypher/queries/interactive-short-3.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 48
  - id: IS4
    contract: CURRENT_ECQR_FRAGMENT
    plan_artifact: IS4.json
    cypher: interactive_v1/cypher/queries/interactive-short-4.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: null
    note: "single-row message-content lookup; predicted trivially agreeing"
  - id: IS5
    contract: CURRENT_ECQR_FRAGMENT
    plan_artifact: IS5.json
    cypher: interactive_v1/cypher/queries/interactive-short-5.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: null
    note: "single-row creator lookup; predicted trivially agreeing"
  - id: IS6
    contract: CURRENT_ECQR_FRAGMENT
    plan_artifact: IS6.json
    cypher: interactive_v1/cypher/queries/interactive-short-6.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 1
  - id: IS7
    contract: REQUIRES_ORDERED_RESULT
    plan_artifact: IS7.json
    cypher: interactive_v1/cypher/queries/interactive-short-7.cypher
    param_source: "SAMPLED anchor (seed 5724984519806421702) + validation_params-sf1.csv"
    arm: characterization-interactive
    d1_local_rows: 1

gates:
  G-R1_every_attempted_has_a_result_or_refusal:
    description: every one of the 24 templates produces either a completed
      Neo4j result (rows, possibly empty) or a recorded refusal (timeout,
      APOC/import failure, same-data-gate abort) — never a silent skip
    max_silent_skips: 0
  G-R2_record_validates:
    description: the run's top-level manifest validates against
      benchmarks/schema/result_manifest.schema.json (RUNBOOK.md §9)
    schema: benchmarks/schema/result_manifest.schema.json
  G-R3_same_data:
    description: the same-data gate (RUNBOOK.md §4.2) passes before any
      query is run; a failure here aborts the whole campaign, not just the
      gate's own row
    required_counts: "2,997,352 nodes / 17,196,776 edges"

# --------------------------------------------------------------------------
# PRE-REGISTERED predictions, scored as-written. A miss is reported, never
# retuned. Per-template predictions are derived from d1_local_rows above
# (basis cited per row); the four templates with no SF1 record
# (BI6/IS1/IS4/IS5) are explicitly labelled "no prior record" and given a
# qualitative rather than a numeric bar.
# --------------------------------------------------------------------------
predictions:
  per_template:
    basis: >-
      benchmarks/results-v1/ldbc-sf1-campaign.json (D1-local SF1 campaign,
      21/24 executed, 0 compared against any reference — this run closes
      the comparison gap, not the execution gap). For each template with a
      d1_local_rows value above, predicted attempted=compared=agreeing=that
      value (i.e. full agreement, no MAPPING-RULE/TGIR-SEMANTIC-GAP/
      ENGINE-DEFECT disagreement) UNLESS the template carries a `risk` note
      above, in which case agreement is predicted but named as the least
      confident bet in the set. BI6/IS1/IS4/IS5 (no d1_local_rows): BI6
      predicted to agree wherever v2 returns rows (D2's corrected triage);
      IS1/IS4/IS5 predicted trivially agreeing (CURRENT_ECQR_FRAGMENT,
      SINGLE_ROW, one entity_history call each per classification.json).
    predicted: >-
      24/24 templates attempted; 18/24 (every template with no `risk` note
      above, plus BI6/IS1/IS4/IS5) predicted to agree on every compared row;
      6/24 (BI3, BI10, BI17, IC5, IC12 — the design memo §7 var-length/path
      residual list — plus no sixth beyond those five, so read as 5/24) are
      the templates where a TGIR-SEMANTIC-GAP or ORDERING/TIE finding on
      some rows would not be a surprise
    pass_if: "every one of the 19 non-risk-flagged templates (all 24 minus
      BI3/BI10/BI17/IC5/IC12) shows zero disagreeing rows"
    refuted_if: "any non-risk-flagged template shows a disagreeing row with
      cause other than ORDERING/TIE or REFERENCE-SIDE-QUIRK"
  overall_agreement:
    basis: >-
      sum of d1_local_rows across the 20 templates that have one = 733 rows
      (607 BI + 66 IC + 60 IS, arithmetic over the table above); BI6.v2 and
      IS1/IS4/IS5 add an unknown but small number of further rows (single-
      row templates plus one unconstrained BI6.v2 count)
    predicted: ">= 0.95 of all compared rows agree, across all 24 templates,
      pooling every row from every template (not per-template weighted)"
    pass_if: ">= 0.90 of all compared rows agree"
    refuted_if: "< 0.75 of all compared rows agree, OR any single template
      disagrees on > 10% of its own compared rows without a documented
      cause from the closed set (MAPPING-RULE/ORDERING-TIE/
      TGIR-SEMANTIC-GAP/ENGINE-DEFECT/REFERENCE-SIDE-QUIRK)"
  bi6_is_not_a_semantic_gap:
    basis: docs/design/BI6_TRIAGE_2026-09-14.md — verdict "plan defect, not
      a TGIR semantic gap"; BI6.v2.json's structural test asserts no join
      key is nullable in its own input's schema
    predicted: "BI6.v2 agrees with the Neo4j reference wherever both sides
      return rows (i.e. is never classified TGIR-SEMANTIC-GAP)"
    pass_if: "BI6.v2's verdict has zero TGIR-SEMANTIC-GAP-caused disagreements"
    refuted_if: "any BI6.v2 disagreement is classified TGIR-SEMANTIC-GAP —
      this would mean D2's triage was itself wrong, not just BI6.json"

falsifiers:
  a: a Neo4j reference result exists for a template whose TGIR plan cannot
     express the query at all (as opposed to a same-row disagreement) —
     this would mean one of the 24 was never actually expressible
  b: a row classified MAPPING-RULE or REFERENCE-SIDE-QUIRK (i.e. "explained
     away") that, on inspection, is actually a wrong TGIR answer — a
     documented divergence rule hiding a defect rather than describing one
  c: the same-data gate (G-R3) fails — the two CSV serializations are not
     the same generated dataset, invalidating every comparison in the run
  d: BI10 (or any template) times out at the 600s ceiling on the reference
     side on every attempt, making zero comparison possible for that
     template — reported as NOT-ATTEMPTED, not folded into the agreement
     denominator
  e: more than 2 of the 5 risk-flagged templates (BI3, BI10, BI17, IC5,
     IC12) show a TGIR-SEMANTIC-GAP disagreement — this would mean the
     var-length/path-feature residual the design memo named as a risk (§7)
     is systematic rather than isolated

# No addenda yet — this is the initial freeze. Any change to `templates`,
# `neo4j`, `gates`, `predictions` or `falsifiers` above lands as a dated
# `addendum_N` block below, never as an edit to the blocks above.

# ==========================================================================
addendum_1:
  date: 2026-09-17
  by: xzgpu execution lane (ldbc-ref-v1 run of record)
  what: >-
    Scoring of the frozen predictions above against the executed run. Appended,
    never edited into the blocks above. The full account is README.md next to
    this file; the run's own records are compare-2026-09-17.json,
    timings-2026-09-17.json and manifest-2026-09-17.json.
  run:
    host: xzgpu
    commit: 0b7198dfdf7a-dirty   # five uncommitted harness fixes, README §6
    neo4j: 5.26.0 + apoc-5.26.0-core, Temurin 21.0.12+8, driver 5.28.6
    import_wall_s: 42
    index_wall_s: 14
    tgms_wall_s: 8187.2
    store: stores/snb-sf1

  gates:
    G-R1_every_attempted_has_a_result_or_refusal: PASS   # 24/24; BI6.v2's ceiling hit is a recorded TIMEOUT, not a skip
    G-R2_record_validates: PASS                          # check_result_manifest.py: conforms
    G-R3_same_data: PASS                                 # 2,997,352 nodes / 17,196,776 edges, both serializations, id digests equal

  # ------------------------------------------------------------------
  # The scoring bar below is `predictions:`' own, applied as written. It is
  # applied to a REDUCED denominator, because 8 of the 24 templates produced
  # no valid comparison for reasons that are properties of the harness and of
  # a frozen host, not of TGIR:
  #   - 7 (BI4 BI9 BI11 BI12 IC2 IC5 IC9) have an INVALID REFERENCE SIDE: their
  #     temporal parameters reached Neo4j as ISO-8601 strings, and
  #     `DATETIME > STRING` yields null rather than raising, so the reference
  #     returned 0 or 1 rows silently. Fix written and tested
  #     (ldbc_reference_run.driverize_params) but NOT executed: host compute
  #     was frozen for a 6-hour soak before the reference could be re-run.
  #   - 1 (BI6, i.e. BI6.v2) hit the 1020 s TGIR ceiling and produced no rows.
  # Scoring those 8 either way would be inventing evidence, so they are
  # reported NOT SCOREABLE rather than folded into any ratio.
  # ------------------------------------------------------------------
  scoring:
    per_template:
      verdict: REFUTED
      on: IS3
      detail: >-
        `refuted_if` reads "any non-risk-flagged template shows a disagreeing
        row with cause other than ORDERING/TIE or REFERENCE-SIDE-QUIRK". IS3 is
        non-risk-flagged and disagrees: TGIR returns 48 rows where Neo4j
        returns 24, exactly 2x, because the store encodes one KNOWS CSV row as
        two edge versions (snb_loader `both_ways=True`, M7) and
        interactive-short-3.cypher matches `-[:KNOWS]-` undirected. The cause
        is neither ORDERING/TIE nor REFERENCE-SIDE-QUIRK, so the prediction is
        refuted as written. RUNBOOK.md §4.3's own M7 line pre-classifies this
        outcome ("any KNOWS-count doubling in a result is a defect to report,
        not a normalization to apply"), and the prior P-SF1b record shows the
        same 48, so it is a standing property of the M7 encoding rather than a
        regression introduced by this run. Reported, not normalized away.
      not_scoreable: [BI4, BI9, BI11, BI12, IC2, IC5, IC9, BI6]
    overall_agreement:
      verdict: NOT SCOREABLE
      detail: >-
        The bar pools every compared row across all 24 templates; with 8
        templates uncomparable the pooled ratio would be a statement about the
        harness, not about agreement. Reported instead, over the 16 templates
        that DID produce a valid comparison: 281 of 329 compared rows agree
        (0.854), where the 48 disagreeing rows are IS3's doubling alone. That
        figure is itself conditional on a column correspondence table that this
        lane did NOT invent (see below), so it is reported as an upper-bound
        reading of the evidence and not as a score against the frozen bar.
    bi6_is_not_a_semantic_gap:
      verdict: NOT SCOREABLE (vacuous)
      detail: >-
        `pass_if` is "BI6.v2's verdict has zero TGIR-SEMANTIC-GAP-caused
        disagreements". BI6.v2 timed out at the 1020 s ceiling and produced no
        rows, so it has zero disagreements of every cause and passes
        vacuously. That is not evidence for D2's triage and is not claimed as
        such. BI6.json (the v1 artifact) did ERROR at SF1, exactly as RUNBOOK
        §6 said it should, so the v1 defect is confirmed to still reproduce.

  falsifiers:
    a_inexpressible_template: NOT TRIGGERED
    b_explained_away_row_hiding_a_defect:
      status: AT RISK — flagged deliberately
      detail: >-
        This run explains several disagreements as harness artifacts (string
        temporal parameters; a `ts` column kind that never fired; divergent
        column names). Each is evidenced rather than asserted — the IS1 rows
        are the same person differing only by exactly 1000x, the BI3/BI7/BI10/
        IS2 rows are byte-identical to the reference under positional column
        pairing — but the coordinator should audit them before any of this is
        cited, since "explained away" is precisely what falsifier b warns of.
    c_same_data_gate_fails: NOT TRIGGERED   # G-R3 PASS
    d_reference_side_timeout:
      status: ANALOGUE TRIGGERED, other side
      detail: >-
        No reference-side timeout occurred anywhere; BI10, the template named
        as most likely to approach the 600 s ceiling, ran in 0.29 s, and the
        whole Neo4j timed range is 0.007-1.974 s. The timeout fell on the TGIR
        side instead (BI6.v2, 1020 s), and is reported NOT-ATTEMPTED for
        comparison purposes rather than folded into the agreement denominator,
        which is the treatment falsifier d prescribes.
    e_systematic_path_residual:
      verdict: NOT TRIGGERED
      detail: >-
        0 of the 5 risk-flagged templates (BI3 BI10 BI17 IC5 IC12) show a
        TGIR-SEMANTIC-GAP disagreement. BI3 and BI10 return values identical to
        the reference; BI17 returns 0 rows on both sides; IC5's reference is
        invalid (0 rows both sides regardless); IC12 agrees on all 5 rows of
        the 4 columns it projects. The design memo §7 var-length/path residual
        did NOT materialise as a systematic effect at SF1.

  caveats_for_the_reader:
    - >-
      `ldbc_compare.py` matches columns BY NAME on both sides despite its own
      docstring ("by column kind, never by column name"), and 10 templates'
      two sides use different names (BI3's `forumId` vs `forum.id`, BI7's
      `relatedTagCount` vs `count`, ...). Four of them (BI3 BI7 BI10 IS2) are
      byte-identical to the reference once columns are paired positionally and
      the µs/ms rule is applied, but positional pairing is NOT universally
      safe — IC8 carries the same names in a different order, where name-based
      matching is right and positional matching would be wrong. A column
      correspondence table is a design decision and was deliberately left to
      the coordinator rather than improvised mid-run.
    - >-
      The comparator only compares the columns the TGIR schema declares, so a
      reference column the TGIR plan does not project is invisible to it.
      IC12's `tagNames` and three of BI4's five columns fall in this hole, and
      IC12's `agreeing` verdict is over-generous as a result.
    - >-
      Wall times on the two sides are NOT a speed ratio: Neo4j ran 1 warm-up +
      3 timed executions against a warm server, TGIR ran its own warmups-1/
      reps-3 protocol (1 rep when bypassed) and its `ms` excludes a 150-270 s
      per-plan store open. They are reported per side and never divided.

# ==========================================================================
addendum_2:
  date: 2026-09-17
  by: xzgpu execution lane, after the coordinator's ratifications
  what: >-
    Re-scoring of the same frozen predictions with the ratified column
    correspondence applied. Appended; addendum_1 is left exactly as written,
    including the readings it reached before the correspondence existed. Same
    row dumps, same reference results, same parameters — only the comparator
    changed, and only in the two ways the coordinator ratified: it now looks
    reference columns up through column_map.yaml, and it reports a reference
    column no TGIR column projects as its own class instead of ignoring it.
  changed_since_addendum_1:
    - >-
      benchmarks/ldbc-ref-v1/column_map.yaml, generated by
      scripts/ldbc_column_map.py from each plan artifact's own Plan.out_schema
      and each vendored .cypher's final RETURN aliases. Rule A (by name) for
      10 templates, rule B (positional, kind-checked) for 11, `unmatched` for
      3. IC8 resolves by name (same six names, different order — positional
      pairing there would compare a timestamp against an id); BI3 by position.
      tests/test_ldbc_column_map.py regenerates the table and asserts byte
      equality, and pins the IC8 and BI3 cases by name.
    - >-
      ldbc_compare.py reports `reference-column-not-projected` where the
      reference projects a column no TGIR column maps to, instead of comparing
      a strict subset of the answer and calling it agreement.
  result_digest_now: 5263399e0f7109aabdbb56e92bda3b6e8d949df4f6e85f194eaeb748ddced169

  verdict_counts:
    agree: 14                                  # BI3 BI7 BI10 BI17 BI18 IC6 IC8 IC11 IS1 IS2 IS4 IS5 IS6 IS7
    reference_column_not_projected: 3          # BI4 IC5 IC12
    disagreeing_but_not_comparable: 5          # BI9 BI11 BI12 IC2 IC9 -- invalid reference, see addendum_1
    disagreeing_genuine: 1                     # IS3
    timeout: 1                                 # BI6.v2, at the pre-registered ceiling
    error: 0
    # BI4 and IC5 carry BOTH an unprojected reference column and an invalid
    # reference; the 7 invalid-reference templates split 5/2 across the two
    # classes above.

  scoring:
    per_template:
      verdict: REFUTED
      on: IS3
      unchanged_from: addendum_1
      detail: >-
        The correspondence moved BI3, BI7, BI10 and IS2 from "every row faults"
        to full agreement, and moved IC12 from a too-generous `agreeing` to
        `reference-column-not-projected`. Neither changes the scoring: IS3 is
        still a non-risk-flagged template disagreeing with a cause that is
        neither ORDERING/TIE nor REFERENCE-SIDE-QUIRK, which is what
        `refuted_if` names. Cause MAPPING-RULE, ratified: the store
        materialises KNOWS both ways (M7) and IS3.json expands
        `dir="both"` over it, so each friendship is traversed twice -- 48 rows
        against the reference's 24. The plan COULD deduplicate: expanding
        `dir="out"` alone reaches every friend exactly once given the
        both-ways materialisation, or a Distinct on
        (friendId, friendshipCreationDate) above the expand. Fix belongs in
        IS3.json; not an engine defect, not a reference quirk.
      not_scoreable: [BI4, BI9, BI11, BI12, IC2, IC5, IC9, BI6]
    overall_agreement:
      verdict: NOT SCOREABLE
      unchanged_from: addendum_1
      detail: >-
        Still not scoreable against the frozen bar, which pools every compared
        row across all 24 templates: 7 templates have no valid reference and 1
        timed out. Over the 16 templates that DID produce a comparison,
        282 of 329 compared rows agree (0.857), every one of the 47
        disagreeing rows being IS3's duplicates. That is now a measurement
        rather than the conditional reading addendum_1 had to give, since the
        column correspondence it depended on is ratified and tested -- but the
        denominator is still two thirds of the campaign, so it is reported and
        not scored. Had the bar been applied to this subset it would sit
        between `pass_if` (>= 0.90) and `refuted_if` (< 0.75), i.e. neither.
    bi6_is_not_a_semantic_gap:
      verdict: NOT SCOREABLE (vacuous)
      unchanged_from: addendum_1
      detail: >-
        BI6.v2 timed out at the pre-registered 1020 s ceiling (600 s bypass +
        420 s store-open allowance) and the ceiling STANDS -- not re-budgeted.
        It therefore has zero disagreements of every cause and passes
        vacuously, which is not evidence for D2's triage and is not claimed as
        such. Noted as a build-to-build difference rather than a budgeting
        question: the same artifact COMPLETED on the a6b3e94 build in
        291,970.9 ms measured (638.4 s child wall) --
        benchmarks/results-v1/ldbc-sf1-campaign-fmt3-2026-09.json, store
        snb-sf1-xz-a6b3e94 -- so the same plan over the same data went from
        ~292 s to over the ceiling between builds. Worth a bisect; out of
        scope for this lane.

  falsifiers:
    b_explained_away_row_hiding_a_defect:
      status: RESOLVED for the column-name class
      detail: >-
        addendum_1 flagged this deliberately, because four templates were being
        explained away on this lane's own reading. The explanation is now a
        generated, tested artifact derived from frozen inputs
        (column_map.yaml), reviewed and ratified by the coordinator, and the
        one class it could have hidden -- a reference column silently dropped
        -- is now its own reported verdict rather than an absence. The
        remaining exposure is the 7 invalid-reference templates, which are
        excluded rather than explained.
    e_systematic_path_residual:
      verdict: NOT TRIGGERED (strengthened)
      detail: >-
        0 of the 5 risk-flagged templates (BI3 BI10 BI17 IC5 IC12) show a
        TGIR-SEMANTIC-GAP. With the correspondence applied BI3 and BI10 now
        agree on every one of their 20 and 100 rows outright, rather than
        merely "having identical values" as addendum_1 could only say. IC12's
        finding is a missing projected column, not a path residual.
    a_inexpressible_template: NOT TRIGGERED
    c_same_data_gate_fails: NOT TRIGGERED
    d_reference_side_timeout: ANALOGUE ON THE TGIR SIDE (BI6.v2), as addendum_1
