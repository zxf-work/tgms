.venv/
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/
.coverage
data_raw/
*.duckdb
*.kuzu/
stores/
.ruff_cache/
dist/
.DS_Store

# user-local Claude Code settings
.claude/
# eval artifacts (result rows, server logs) live outside version control
runs/

# CIDR submission draft — not public yet
paper/cidr/

# phase-3 private planning handoff
HANDOFF-PHASE3.md

# rust build artifacts
target/
# compiled engine extension (built into the source tree by maturin editable installs)
*.so
*.pyd

# phase-3 planning and design-review notes (internal; reference the
# unpublished CIDR draft and PI-side items — this repo is public)
PHASE3_SPEC.md
TGMS_NATIVE_ENGINE_REVIEW.md
TGMS_NATIVE_ENGINE_V2_REVIEW.md
TGMS_NATIVE_ENGINE_EVALUATION_PLAN.md

# --- internal working documents: never published (policy, 2026-07-31) ---
# Plans, blueprints, decision logs, roadmaps, and milestone notes stay
# local. Public docs are: the website (docs/index.html, docs/blog/), the
# community lessons doc, evaluation results + docs/eval deliverables.
ENGINE_BLUEPRINT.md
ENGINE_IMPLEMENTATION_SPEC.md
docs/ROADMAP.md
docs/DECISIONS.md
docs/design/
docs/engine_evaluation_readiness.md
docs/engine_probe.md
docs/bench_ops.md
docs/TECHNICAL_REPORT.md
docs/RELATED.md
PHASE3_SPEC.md
TGMS_NATIVE_ENGINE_*.md
HANDOFF-*.md
docs/first_oss_matrix_devrun.md

# papers in progress — internal repo only (tracked via `git tgi`); the ECQR
# submission and the TGIR preprint must never reach the public remote.
# (Added 2026-08-30 after an `add -A` swept them into a public push that
# had to be force-rewritten — a gitignore is the structural guarantee.)
paper/
# internal review/handoff work products at the repo root
ECQR_*.md
TGMS_Contributor_Review_*.md
