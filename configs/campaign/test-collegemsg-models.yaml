# C3 model-scaling readout on the frozen test split: ours only, one seed;
# 14B numbers come from test-collegemsg-main. Serve each model in turn
# (scripts/run_campaign.sh handles the swap).
suite_path: stores/suite-collegemsg/suite.json
store_path: stores/collegemsg
out_dir: runs/test-collegemsg-models
split: test
systems: [ours]
models: ["openai/Qwen/Qwen2.5-7B-Instruct"]
seeds: [0]
max_repairs: 3
memory_db: stores/collegemsg/memory.sqlite
llm_api_base: "http://localhost:8000/v1"
llm_api_key: "EMPTY"
